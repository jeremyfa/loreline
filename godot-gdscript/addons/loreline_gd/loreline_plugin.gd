@tool
extends EditorPlugin

## Editor plugin for the pure-GDScript Loreline runtime. The runtime works
## without enabling the plugin (all classes register via class_name); this
## exists so the addon shows up in the project's plugin list.

func _enter_tree() -> void:
	pass


func _exit_tree() -> void:
	pass
